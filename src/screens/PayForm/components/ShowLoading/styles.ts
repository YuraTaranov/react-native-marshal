import {StyleSheet} from 'react-native';
import {sizes} from '@constants';

export default StyleSheet.create({
  container: {
    flex: 1,
    height: 300,
  },
  contentContainer: {
    flex: 1,
  },
});
