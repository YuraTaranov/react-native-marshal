import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  buttonFilter: {
    width: 50,
    height: 50,
    position: 'absolute',
    right: 0,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
